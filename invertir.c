#include <stdio.h>
#include <string.h>

void invertir(char *s) {
	size_t i=0, j=strlen(s);
	if (j>0 && s[j-1] == '\n') j--;
	while (i<j) { char t=}
}

int main() {
	
	for (int i=0; i < 5; i++){
		if (i%2 == 0) {
			printf("%d es par\n", i);
		} else {
			printf("%d es impar\n", i);
		}
	}
	printf("Hola mundo desde C\n");
	saludar("Ana");
	return 0;

}
